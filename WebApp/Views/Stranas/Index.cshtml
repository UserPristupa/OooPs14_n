@model IEnumerable<WebApp.Strana>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.name_stran)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.kod_stran)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.post_index_stran)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name_stran)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.kod_stran)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.post_index_stran)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id_strana }) |
                @Html.ActionLink("Details", "Details", new { id=item.id_strana }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id_strana })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
